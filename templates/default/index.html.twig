{% extends('base.html.twig') %}

{% block title %} Index {% endblock %}

{% block body %}
<h1>{{ prenom }} </h1>
<h4>{{ age }}</h4>

        <ul>
    {% for fruit in fruits %}
        <li>{{ fruit }}</li>
    {% endfor %}
</ul>
{# commentaires #}
<h6>nb fruits  {{ fruits|length }} </h6>
{% endblock body %}
